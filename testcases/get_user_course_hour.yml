- config:
    name: dubbo testcase template

- test:
    name: get user course hour
    api: api/get_user_course_hour.yml
    variables:
      args: [list, str1, str2, str3]
    extract:
      - leftover: giftHourLeftover
      - total: giftHourTotal
      - xxx: gift.xxx
    validate:
      - eq: [giftHourLeftover, 3]
      - eq: [giftHourTotal, 3]
