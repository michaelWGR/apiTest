name: 获取学员课时信息Rpc接口

variables:
  userId: 600018

request:
  host: ${yml(rpc.host)}
  port: ${yml(rpc.port)}
  package: 'com.i61.live.hll.api.HllGetUserCourseHourService'
  method: 'getUserCourseHour'
  args: $userId