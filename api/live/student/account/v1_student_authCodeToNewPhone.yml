name: 获取验证码（换账号用）
base_url: ${yml(student.base_url)}

variables:
  account: ${yml(student.account.account1)}
  deviceId: ${yml(student.default_device_id)}
  token: null
  expected_status_code: 200

request:
  url: /v1/student/authCodeToNewPhone
  method: GET
  headers:
    Authorization: $token
  params:
    phoneNum: $account
    deviceid: $deviceId

validate:
  - eq: ["status_code", $expected_status_code]