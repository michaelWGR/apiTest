name: 免验证获取简单学生信息接口
base_url: ${yml(student.base_url)}

variables:
  phone: ${yml(student.account.account1)}
  expected_status_code: 200

request:
  url: /v1/student/simpleUserInfo
  method: GET
  params:
    phone: $phone

validate:
  - eq: ["status_code", $expected_status_code]