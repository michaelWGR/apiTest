name: floating log
base_url: ${yml(student.base_url)}
variables:
    deviceId: ${yml(student.default_device_id)}
    Authorization: null
    url: null
    expected_status_code: 200
request:
    url: /v1/student/app/floating/log
    method: GET
    headers:
        Authorization: $Authorization
    params:
        deviceId: $deviceId
        url: $url


validate:
    - eq: ["status_code", $expected_status_code]
